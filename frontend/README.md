# Smart Telehealth Frontend

Static frontend for the AWS telehealth demo.

## Local Build

```bash
npm ci
npm run build
npm start
```

Open `http://localhost:5173`.

After deployment, paste the ALB URL into the backend URL field, for example:

```text
http://your-alb-dns-name
```
